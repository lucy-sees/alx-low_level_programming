#include "main.h"
/**
 *main -prints 1 or 0 depending on the input
 *@c: 1st parameter
 *
 *Description: prints all lowercase letters
 *Return: Always(0).
 */
/*function prototype*/
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
